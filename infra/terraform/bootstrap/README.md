# Bootstrap Module

Mirror module for `cv` structure.

In production, bootstrap resources should be managed from `platform-ops`.
Keep this module as a compatibility placeholder unless bootstrap ownership is moved into `gpool`.
